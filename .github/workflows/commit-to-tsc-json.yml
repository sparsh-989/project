name: Check Commit Author and Changes

on:
  push:
    branches:
      - main
    paths:
      - 'tsc.json'
  pull_request:
    branches:
      - main
    paths:
      - 'tsc.json'

jobs:
  check-author:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: 16

      - name: Install dependencies for script
        working-directory: .github/workflows
        run: npm ci

      - name: Install dependencies for getPRChanges
        working-directory: .github/workflows/getPRChanges
        run: npm ci

      - name: Get commit SHA
        id: get_sha
        run: |
          if [ "${{ github.event_name }}" == "push" ]; then
            echo "${{ github.sha }}" >> commit_sha.txt
          else
            echo "${{ github.event.pull_request.head.sha }}" >> commit_sha.txt
          fi
          echo "::set-env name=COMMIT_SHA::$(cat commit_sha.txt)"
      - name: Run script
        id: author_check
        env:
          COMMIT_ID: ${{ env.COMMIT_SHA }}
        run: |
          USER_TYPE=$(node .github/workflows/script.js $COMMIT_ID)
          echo "::set-env name=USER_TYPE::$USER_TYPE"
      - name: Get PR changes
        if: ${{ github.event_name == 'pull_request' }}
        id: get_pr_changes
        env:
         PULL_REQUEST_NUMBER: ${{ github.event.pull_request.number }}
        run: |
          PR_CHANGES=$(node .github/workflows/getPRChanges/getPRChanges.js)
          echo "$PR_CHANGES" >> pr_changes.json
          echo "${{ github.event.pull_request.number }}" >> pull_request_number.txt
          echo "::set-env name=PR_CHANGES::$(cat pr_changes.json)"
          echo "::set-env name=PULL_REQUEST_NUMBER::$(cat pull_request_number.txt)"
      - name: Check changes
        if: ${{ env.USER_TYPE == 'The user is a human.' }}
        run: |
          git diff ${{ github.event.pull_request.base.sha }} ${{ env.COMMIT_SHA }} -- tsc.json > changes.diff
          cat changes.diff
      - name: Block PR if name or repos changed
        if: ${{ env.USER_TYPE == 'The user is a human.' }}
        run: |
          CHANGES_FOUND=$(grep -E '^\+' changes.diff | grep -E '("name"|"repos")')
          if [ ! -z "$CHANGES_FOUND" ]; then
            echo "Blocking PR: Changes detected in 'name' or 'repos'"
            exit 1
          else
            echo "No changes in 'name' or 'repos' detected"
          fi
      - name: Approve PR if slack, linkedin, or twitter changed
        if: ${{ env.USER_TYPE == 'The user is a human.' }}
        run: |
          CHANGES_FOUND=$(grep -E '^\+' changes.diff | grep -E '("slack"|"linkedin"|"twitter")')
          if [ ! -z "$CHANGES_FOUND" ]; then
            echo "Approving PR: Changes detected in 'slack', 'linkedin', or 'twitter'"
            # Add logic here to approve the PR if needed
          else
            echo "No changes in 'slack', 'linkedin', or
