name: Commit to tsc.json

on:
  push:
    branches:
      - main
    paths:
      - 'tsc.json'

jobs:
  display-commit-id:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v2

      - name: Display commit ID and message
        run: |
          echo "Hello"
          echo "Commit ID: ${{ github.sha }}"
